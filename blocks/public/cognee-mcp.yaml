name: Cognee MCP
version: 1.0.2
schema: v1

mcpServers:
  - name: Cognee MCP
    command: docker
    args:
      - run
      - -i
      - --rm
      - -v ${{ inputs.COGNEE_SHARED_DIR_PATH }}:/app/data
      - --env-file ${{ inputs.COGNEE_ENV_FILE_PATH }}
      - cognee/cognee-mcp:main
